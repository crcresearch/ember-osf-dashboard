<!-- app/templates/application.hbs -->
<div class="context">
    {{partial 'sidebar'}}
    <div class="pusher">
        {{partial 'navbar'}}
        <div class="ui container pusher">
            {{outlet}}
        </div>
    </div>
</div>
